# jaetechlab.com 연결 · 애드센스 검토 요청 안내서

사용자가 직접 해야 하는 일만 순서대로 적었습니다. 사이트 자체는 `docs/` 에 완성되어 있고 GitHub Pages 에 올라가 있습니다.

## 0. 현재 상태 확인

- GitHub 저장소: `https://github.com/seungwoopapa/jaetechlab`
- 임시 주소: `https://seungwoopapa.github.io/jaetechlab/` 는 쓰지 않습니다. CNAME 파일이 `jaetechlab.com` 으로 잡혀 있어서 도메인 연결 후에만 정상 동작합니다. 도메인 연결 전에는 로컬 미리보기로 확인하세요.

```bash
cd ~/jaetechlab-restore && python3 -m http.server 8765 --directory docs
```
브라우저에서 `http://localhost:8765` 를 열면 됩니다.

## 1. 블로거(Blogspot) 맞춤 도메인 해제

지금 `jaetechlab.com` 은 옛 블로그스팟("이모저모")으로 301 리다이렉트됩니다. 새 사이트가 보이려면 이 연결을 먼저 끊습니다.

1. blogger.com 로그인 → 해당 블로그 → **설정** → **게시** → **맞춤 도메인** 옆의 ✕(삭제).
2. 저장. 블로그는 원래 `xxx.blogspot.com` 주소로만 남습니다. 글은 지워지지 않습니다.

## 2. 가비아 DNS 변경

가비아 → My가비아 → 도메인 → `jaetechlab.com` → **DNS 관리** → **DNS 설정**.

### 지울 레코드
- 블로그스팟용 레코드 전부: `ghs.google.com` 을 가리키는 CNAME, `googlehosted.com` 계열 CNAME, 옛 워드프레스 호스팅 IP 를 가리키는 A 레코드.

### 추가할 레코드

| 타입 | 호스트 | 값 | TTL |
|---|---|---|---|
| A | @ | 185.199.108.153 | 3600 |
| A | @ | 185.199.109.153 | 3600 |
| A | @ | 185.199.110.153 | 3600 |
| A | @ | 185.199.111.153 | 3600 |
| CNAME | www | `seungwoopapa.github.io.` | 3600 |

- 가비아는 A 레코드를 같은 호스트(@)로 4개 따로 추가하면 됩니다.
- CNAME 값 끝의 점(`.`)은 가비아가 자동으로 붙이기도 합니다. 저장 후 표시되는 값이 `seungwoopapa.github.io` 이면 됩니다.
- 나중에 `xx.jaetechlab.com` 서브도메인을 티스토리 2차 도메인으로 쓸 때는 서브도메인별 CNAME 을 추가하면 되고, 위 레코드와 충돌하지 않습니다.

반영은 보통 10분~1시간, 길면 하루입니다. 확인:

```bash
dig +short jaetechlab.com A
```
185.199.108~111.153 네 개가 나오면 완료입니다.

## 3. GitHub Pages 에서 도메인 확인 · HTTPS 켜기

1. `https://github.com/seungwoopapa/jaetechlab/settings/pages` 접속.
2. **Custom domain** 에 `jaetechlab.com` 이 들어 있고 "DNS check successful" 이 뜨는지 확인. (CNAME 파일이 이미 있어서 자동으로 채워집니다. 비어 있으면 직접 입력 후 Save.)
3. DNS 확인이 끝나면 **Enforce HTTPS** 체크. 인증서 발급에 몇 분~1시간 걸립니다.
4. `https://jaetechlab.com/` 과 `https://www.jaetechlab.com/` 둘 다 열리는지, `https://jaetechlab.com/ads.txt` 가 텍스트 한 줄을 보여 주는지 확인.

## 4. 서치콘솔 등록 · 사이트맵 제출 (검토 요청 전에 꼭)

애드센스 심사는 구글이 사이트를 크롤링한 결과로 판단합니다. 새 콘텐츠가 색인되기 전에 검토를 요청하면 예전 블로그스팟 기준으로 다시 반려될 수 있습니다.

1. search.google.com/search-console → 속성 추가 → **도메인** 방식으로 `jaetechlab.com` 입력.
2. 안내되는 TXT 레코드를 가비아 DNS 에 추가(호스트 @, 타입 TXT, 값 `google-site-verification=...`). 확인 버튼.
3. 색인 생성 → **사이트맵** → `https://jaetechlab.com/sitemap.xml` 제출.
4. **URL 검사**에 홈·글 서너 개 주소를 넣고 "색인 생성 요청". 하루 이틀 뒤 "페이지" 보고서에서 색인된 페이지 수가 올라가는지 확인.

기존에 `jaetechlab.com` 속성이 이미 있으면 사이트맵만 다시 제출하면 됩니다.

## 5. 애드센스 검토 요청

1. adsense.google.com → **사이트** → `jaetechlab.com` (상태 "주의 필요").
2. 사이트 모든 페이지 `<head>` 에 애드센스 코드가 들어 있습니다(`config.json` 의 `adsense_client` = `ca-pub-8336147776969406`, jaetechlab.com 을 등록한 계정의 게시자 ID 와 같음을 2026-09-11 확인).
3. `ads.txt` 도 같은 ID 로 자동 생성되어 있습니다. 애드센스가 "ads.txt 문제" 를 표시하면 1~2일 기다리면 사라집니다.
4. 서치콘솔에서 색인된 페이지가 20개 이상 확인된 뒤 **검토 요청** 버튼을 누릅니다. 결과는 보통 며칠, 길면 2~4주.
5. 결과가 나오면 구대리 세션(계정·수익)에 알려서 계정마스터에 등록합니다.

### 반려가 다시 나오면
- "가치가 별로 없는 콘텐츠": 서치콘솔 색인 수를 확인하고, 색인이 덜 됐으면 2주 더 기다렸다가 재요청. 그래도 반려면 새 글 3~5편을 추가하는 것이 가장 확실합니다(`content/posts.json` 에 항목 추가 → `build.py`).
- "사이트 다운 또는 사용 불가": DNS 나 HTTPS 문제. 3번 단계 재확인.
- "애드센스 코드 없음": `<head>` 의 코드 ID 가 계정과 다른 경우. 2번 단계.

## 6. 그 다음

- 승인 뒤에도 사이트는 그냥 두면 됩니다. 사용자 본인 글이고 유지 비용이 없습니다.
- 글을 더 올리려면 `content/posts.json` 에 항목을 추가하고 `build.py` 를 돌린 뒤 푸시하면 됩니다. 형식은 기존 항목을 복사하면 됩니다.
